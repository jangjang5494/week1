-- ============================================================
-- 소득기준 연도별 업데이트용
-- 사용법: Supabase SQL Editor → New query → 붙여넣기 → Run
-- ============================================================

-- ★ 도시근로자 월평균소득 갱신 시기: 매년 3~4월 (국토부 고시)
-- INSERT INTO income_standards (standard_type, year, household_size, amount) VALUES
--   ('도시근로자월평균', 2026, 1, 여기에값),
--   ('도시근로자월평균', 2026, 2, 여기에값),
--   ('도시근로자월평균', 2026, 3, 여기에값),
--   ('도시근로자월평균', 2026, 4, 여기에값),
--   ('도시근로자월평균', 2026, 5, 여기에값);

-- ★ 기준중위소득 갱신 시기: 매년 8월 고시 → 다음해 1월 적용 (보건복지부)
-- INSERT INTO income_standards (standard_type, year, household_size, amount) VALUES
--   ('중위소득', 2027, 1, 여기에값),
--   ('중위소득', 2027, 2, 여기에값),
--   ('중위소득', 2027, 3, 여기에값),
--   ('중위소득', 2027, 4, 여기에값),
--   ('중위소득', 2027, 5, 여기에값),
--   ('중위소득', 2027, 6, 여기에값);

-- ★ 현재 DB에 들어있는 값 확인용
-- SELECT * FROM income_standards ORDER BY year DESC, household_size;
